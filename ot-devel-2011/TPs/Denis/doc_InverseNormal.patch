commit ac1da0a2f82cd2ab904e656abffbfea4350970c6
Author: Denis Barbier <barbier@imacs.polytechnique.fr>
Date:   Thu Mar 24 12:13:22 2011 +0100

    Add documentation about the InverseNormal distribution

diff --git a/src/UseCasesGuide/docUC_InputNoData_UsualDist.tex b/src/UseCasesGuide/docUC_InputNoData_UsualDist.tex
index 82c26e1..af39c5e 100644
--- a/src/UseCasesGuide/docUC_InputNoData_UsualDist.tex
+++ b/src/UseCasesGuide/docUC_InputNoData_UsualDist.tex
@@ -20,6 +20,7 @@
 \index{Usual Distribution!Gamma}
 \index{Usual Distribution!Gumbel}
 \index{Usual Distribution!Histogram}
+\index{Usual Distribution!Inverse Normal}
 \index{Usual Distribution!Laplace}
 \index{Usual Distribution!Logistic}
 \index{Usual Distribution!LogNormal}
@@ -117,6 +118,10 @@ Details on each object may be found in the User Manual  (\href{OpenTURNS_UserMan
     % 
     \hline
     % 
+    Inverse Normal & $  \displaystyle \sqrt{\frac{\lambda}{2\pi x^3}}^{\strut} e^{\frac{-\lambda(x - \mu)^2}{2\mu^2 x}} $ & $\lambda > 0$, $\mu > 0$ & $(\lambda, \mu)$ & -- \\
+    % 
+    \hline
+    % 
     Laplace & $ \displaystyle \frac{1^{\strut}}{2\lambda_{\strut}}e^{-|x-\mu|/\lambda}$ & $\lambda>0$ & $(\lambda, \mu)$ & -- \\
     % 
     \hline
@@ -359,6 +364,12 @@ The creation of each distribution is described in the following Python script :
   collection = HistogramPairCollection( ( (1.,1.), (4.,2.), (2.,3.), ) )
   histogram = Histogram(0., collection)
 
+  # InverseNormal
+  # Ppal Param = InverseNormal(lambda, mu)
+  inverseNormal = InverseNormal(1., 3.)
+  # Default construction ==> InverseNormal(l, m) = InverseNormal(1.0, 1.0)
+  inverseNormal = InverseNormal()
+
   # Laplace
   # Ppal Param : Laplace(lambda, mu)
   laplace = Laplace(1.5, 0.5)
@@ -668,6 +679,28 @@ The pdf of the usual distributions are drawned in Figures \ref{PDF1}to \ref{PDF2
 \begin{figure}[Hhbtp]
   \begin{minipage}{10cm}
     \begin{center}
+      \includegraphics[width=7cm]{pdf_InverseNormal.png}
+      \caption{PDF of an Inverse Normal distribution.}
+      \label{PDF-InverseNormal}
+    \end{center}
+  \end{minipage}
+  \hfill
+  \begin{minipage}{10cm}
+    \begin{center}
+      \includegraphics[width=7cm]{pdf_InverseNormal2.png}
+      \caption{PDF of an Inverse Normal distribution.}
+      \label{PDF-InverseNormal2}
+    \end{center}
+  \end{minipage}
+\end{figure}
+
+
+
+
+
+\begin{figure}[Hhbtp]
+  \begin{minipage}{10cm}
+    \begin{center}
       \includegraphics[width=7cm]{pdf_Laplace.png}
       \caption{PDF of a Laplace distribution.}
       \label{PDF13}
diff --git a/src/UseCasesGuide/pdf_InverseNormal.png b/src/UseCasesGuide/pdf_InverseNormal.png
new file mode 100644
index 0000000..1c706c1
Binary files /dev/null and b/src/UseCasesGuide/pdf_InverseNormal.png differ
diff --git a/src/UseCasesGuide/pdf_InverseNormal2.png b/src/UseCasesGuide/pdf_InverseNormal2.png
new file mode 100644
index 0000000..d7239d3
Binary files /dev/null and b/src/UseCasesGuide/pdf_InverseNormal2.png differ
diff --git a/src/UseCasesGuide/script_usualDistribution.py b/src/UseCasesGuide/script_usualDistribution.py
index 693a6f0..6b5c1cf 100644
--- a/src/UseCasesGuide/script_usualDistribution.py
+++ b/src/UseCasesGuide/script_usualDistribution.py
@@ -188,6 +188,18 @@ ViewImage(pdf_gumbel.getBitmap())
 
 
 
+# inverse normal
+
+inverseNormal = InverseNormal(1.0, 3.0)
+pdf_inverseNormal =  inverseNormal.drawPDF()
+pdf_inverseNormal.setTitle("PDF - InverseNormal(l, m)")
+draw = pdf_inverseNormal.getDrawable(0)
+draw.setLegendName("InverseNormal(1.0, 3.0)")
+pdf_inverseNormal.setDrawable(draw,0)
+pdf_inverseNormal.draw("pdf_InverseNormal")
+ViewImage(pdf_inverseNormal.getBitmap())
+
+
 # laplace
 
 laplace = Laplace()